# 微信小程序真实接口联调清单

> 适用范围：火锅食材 B2B 小程序端。
>
> 目标：在正式开发和后端联调之前，明确每个页面要调用哪些接口、需要什么参数、返回什么字段、如何处理空状态和异常状态，确保前后端联调高效、稳定、可追踪。

---

## 1. 联调目标

### 1.1 目标

- 明确每个页面的数据来源
- 明确接口输入输出结构
- 明确页面与后端字段映射
- 明确错误、空状态和加载状态处理方式
- 明确联调优先级和顺序

### 1.2 联调原则

1. 页面先有静态骨架，再接真实接口
2. 所有接口必须走统一请求层
3. 所有返回结构必须统一
4. 所有错误必须可见且可追踪
5. 联调从核心闭环开始，不要一开始并行过多功能

---

## 2. 统一接口约定

### 2.1 响应结构

建议所有接口统一返回：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "requestId": "xxx"
}
```

### 2.2 常见状态约定

- `code === 0`：成功
- `401` 或特定鉴权码：登录失效
- `403`：无权限
- `404`：资源不存在
- `500`：服务异常

### 2.3 分页约定

分页接口建议统一：

```json
{
  "page": 1,
  "pageSize": 10,
  "total": 100,
  "items": []
}
```

---

## 3. 页面级联调清单

## 3.1 登录页

### 需要接口

- `POST /api/v1/auth/login`
- `GET /api/v1/auth/profile`

### 登录请求参数

```json
{
  "phone": "13800000000",
  "code": "123456"
}
```

### 登录成功返回建议

```json
{
  "token": "jwt-token",
  "userInfo": {
    "id": 1,
    "name": "王先生",
    "phone": "13800000000",
    "userType": "customer_user"
  }
}
```

### 页面映射

- token 存入本地缓存和 store
- userInfo 存入本地缓存和 store
- 登录成功后跳转首页

### 异常处理

- 验证码错误：提示具体错误
- 手机号格式不正确：前端先校验
- 接口失败：提示重试

---

## 3.2 首页

### 需要接口

- `GET /api/v1/auth/profile`
- `GET /api/v1/brands`
- `GET /api/v1/configs`
- `GET /api/v1/products`
- `GET /api/v1/orders?status=pending`

### 需要的核心数据

- 当前品牌信息
- 用户/客户信息
- 首页配置
- 推荐商品列表
- 待处理订单提示

### 页面映射

- Hero 文案可由配置控制
- 推荐商品来自商品接口
- 订单提醒来自订单接口
- 优势模块可先静态，后续可配置化

### 空状态处理

- 商品为空：展示推荐占位和引导文案
- 订单为空：展示“暂无待处理订单”提示

### 异常处理

- 推荐商品加载失败：展示骨架屏或兜底商品
- 配置获取失败：使用本地默认配置

---

## 3.3 分类页

### 需要接口

- `GET /api/v1/brands`
- `GET /api/v1/products`
- `GET /api/v1/categories`

### 请求参数建议

```json
{
  "brandId": 1,
  "categoryId": "all",
  "keyword": "毛肚",
  "page": 1,
  "pageSize": 10
}
```

### 页面映射

- 分类 tabs 来自分类接口
- 商品列表来自商品接口
- 品牌切换影响商品请求参数

### 空状态处理

- 当前分类无商品：展示空状态和清空筛选引导
- 搜索无结果：展示“未找到相关商品”

### 异常处理

- 接口超时：显示重新加载按钮
- 分类接口失败：使用本地兜底分类

---

## 3.4 商品详情页

### 需要接口

- `GET /api/v1/products/{id}`
- `GET /api/v1/products/{id}/related`
- `GET /api/v1/inventories/{skuId}`

### 请求参数建议

```json
{
  "id": 1001
}
```

### 页面映射

- 商品名、规格、价格、库存状态直接来自详情接口
- 相关商品来自推荐接口
- 库存状态必须明确区分：充足、预警、缺货

### 详情响应建议

```json
{
  "id": 1001,
  "name": "鲜切毛肚",
  "spec": "500g / 袋",
  "price": 88,
  "customerPrice": 82,
  "stockText": "库存充足",
  "images": [],
  "description": "工厂直供，适合火锅门店快速复购"
}
```

### 空状态处理

- 商品不存在：显示商品已下架或不存在
- 无相关商品：隐藏推荐区或显示默认推荐

### 异常处理

- 价格接口失败：显示基础售价和加载提示
- 库存接口失败：显示“库存信息待同步”

---

## 3.5 购物车页

### 需要接口

- `GET /api/v1/cart`
- `POST /api/v1/cart/items`
- `PATCH /api/v1/cart/items/{id}`
- `DELETE /api/v1/cart/items/{id}`

### 请求参数建议

```json
{
  "productId": 1001,
  "skuId": 2001,
  "quantity": 2
}
```

### 页面映射

- 购物车列表
- 数量步进器
- 商品小计
- 总金额
- 去下单按钮

### 空状态处理

- 购物车为空时展示引导页和去分类入口

### 异常处理

- 数量修改失败：回滚 UI 状态
- 删除失败：恢复数据并提示错误

---

## 3.6 下单页

### 需要接口

- `GET /api/v1/addresses/default`
- `GET /api/v1/cart`
- `POST /api/v1/orders/preview`
- `POST /api/v1/orders`

### 请求参数建议

```json
{
  "addressId": 1,
  "remark": "尽量下午送达",
  "items": [
    {
      "productId": 1001,
      "skuId": 2001,
      "quantity": 2
    }
  ]
}
```

### 页面映射

- 收货信息来自地址接口
- 商品清单来自购物车
- 金额汇总来自下单预览接口
- 提交订单来自创建订单接口

### 预览响应建议

```json
{
  "subtotal": 244,
  "discountAmount": 0,
  "adjustAmount": 0,
  "payableAmount": 244
}
```

### 空状态处理

- 无地址：引导新增地址
- 购物车为空：返回购物车页

### 异常处理

- 预览失败：禁止提交按钮可用状态
- 库存不足：明确提示哪个商品不足
- 价格变更：提示重新确认金额

---

## 3.7 订单列表页

### 需要接口

- `GET /api/v1/orders`

### 请求参数建议

```json
{
  "status": "all",
  "page": 1,
  "pageSize": 10
}
```

### 页面映射

- 订单卡片
- 状态标签
- 金额
- 创建时间
- 再次购买入口

### 空状态处理

- 无订单：显示首次下单引导
- 当前筛选无订单：提示切换筛选条件

### 异常处理

- 列表加载失败：显示重试按钮
- 分页加载失败：保留已加载数据

---

## 3.8 订单详情页

### 需要接口

- `GET /api/v1/orders/{id}`
- `GET /api/v1/orders/{id}/timeline`

### 请求参数建议

```json
{
  "id": 202601010001
}
```

### 页面映射

- 订单状态
- 履约时间线
- 商品明细
- 金额汇总
- 收货信息
- 操作入口

### 详情响应建议

```json
{
  "id": 202601010001,
  "orderNo": "202601010001",
  "status": "pending_confirm",
  "totalAmount": 244,
  "items": [],
  "customer": {},
  "address": {},
  "timeline": []
}
```

### 空状态处理

- 订单不存在：提示订单已删除或不存在

### 异常处理

- 时间线接口失败：展示基础状态，不阻塞主内容

---

## 3.9 个人中心页

### 需要接口

- `GET /api/v1/auth/profile`
- `GET /api/v1/addresses`
- `GET /api/v1/customers/me`

### 页面映射

- 用户名
- 手机号
- 客户资料
- 地址管理入口
- 专属价格入口
- 设置入口

### 空状态处理

- 未登录：引导登录
- 没有客户资料：提示联系客服绑定

### 异常处理

- 资料获取失败：允许基础页面展示

---

## 4. 接口优先级

建议按以下顺序联调：

1. 登录接口
2. 用户信息接口
3. 商品列表接口
4. 商品详情接口
5. 购物车接口
6. 地址接口
7. 下单预览接口
8. 创建订单接口
9. 订单列表接口
10. 订单详情接口
11. 品牌接口
12. 配置接口

---

## 5. 字段映射表

### 5.1 商品字段

- `name` → 商品名称
- `spec` → 规格
- `price` → 基础价格
- `customerPrice` → 客户专属价
- `stockText` → 库存状态
- `images` → 商品图集
- `description` → 商品说明

### 5.2 订单字段

- `orderNo` → 订单号
- `status` → 订单状态
- `totalAmount` → 商品总额
- `discountAmount` → 优惠金额
- `adjustAmount` → 调价金额
- `payableAmount` → 应付金额
- `timeline` → 履约时间线

### 5.3 用户字段

- `name` → 用户名
- `phone` → 手机号
- `customerName` → 客户名称
- `customerLevel` → 客户等级

---

## 6. 前端联调约定

### 6.1 请求处理

- 所有页面请求必须通过 `services/request.js`
- 所有接口路径通过 `constants/api.js`
- 所有缓存数据通过 `utils/storage.js`

### 6.2 错误处理

- 失败必须提示
- 空数据必须有视觉反馈
- 登录失效必须跳登录页
- 数据异常必须支持重试

### 6.3 Loading 处理

- 列表请求用骨架屏
- 提交请求用按钮 loading
- 详情页可局部加载

---

## 7. 联调用例建议

### 7.1 登录用例

- 正确手机号 + 正确验证码 → 登录成功
- 正确手机号 + 错误验证码 → 返回错误提示
- token 失效 → 跳转登录页

### 7.2 商品用例

- 有数据 → 正常列表
- 无数据 → 空状态
- 接口失败 → 重试

### 7.3 下单用例

- 库存充足 → 可提交
- 库存不足 → 提示并阻止提交
- 价格发生变化 → 重新确认

---

## 8. 联调验收标准

### 8.1 功能层

- 每个页面接口都已接入
- 数据展示正确
- 空状态正确
- 错误状态正确

### 8.2 体验层

- 加载有反馈
- 提交无重复
- 页面跳转无卡顿
- 登录后状态正常恢复

### 8.3 数据层

- 商品、订单、用户字段映射正确
- 客户专属价能正确展示
- 订单状态能正确渲染

---

## 9. 风险提示

- 后端返回结构不统一会严重拖慢联调
- 商品与订单字段一旦不一致，前端映射会反复返工
- 登录态失效逻辑必须提前统一
- 库存和价格逻辑不要只放前端处理

---

## 10. 结论

这份清单的目的，是让小程序联调阶段变得可控、可排查、可验收。

当这些接口按顺序打通后，小程序就可以从“骨架状态”进入到真正可用的业务状态。
