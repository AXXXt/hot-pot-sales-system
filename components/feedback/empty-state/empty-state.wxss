.empty-state {
  padding: 64rpx 32rpx;
  text-align: center;
  background: var(--glass-bg);
  border: 1rpx solid var(--glass-border);
  border-radius: 32rpx;
  box-shadow: var(--shadow-glass);
  backdrop-filter: var(--glass-backdrop);
  -webkit-backdrop-filter: var(--glass-backdrop);
}

.empty-state__icon {
  width: 96rpx;
  height: 96rpx;
  border-radius: 32rpx;
  background: rgba(17, 24, 39, 0.04);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 24rpx;
  font-size: 36rpx;
  color: var(--text-tertiary);
}

.empty-state__title {
  display: block;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.empty-state__desc {
  display: block;
  margin-top: 12rpx;
  font-size: 24rpx;
  color: var(--text-secondary);
  line-height: 1.7;
}

.empty-state__action {
  display: inline-flex;
  margin-top: 24rpx;
  padding: 18rpx 28rpx;
  border-radius: 9999rpx;
  background: var(--brand-primary);
  color: var(--text-inverse);
  font-size: 24rpx;
  font-weight: 600;
}

.empty-state {
  margin: 24rpx;
  padding: 64rpx 36rpx;
  border-radius: 32rpx;
  background: rgba(255,255,255,0.92);
  box-shadow: var(--shadow-sm);
}
.empty-state__action { background: linear-gradient(135deg, var(--brand-accent), var(--brand-primary)); }
